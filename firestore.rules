rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Blitz Records collection
    match /blitz_records/{recordId} {
      allow read, write: if true;
    }
    
    // App settings collection
    match /app_settings/{settingId} {
      allow read, write: if true;
    }
  }
}
